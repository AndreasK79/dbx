Here's everything added to the source tree, in order — all of it verified (tests + typecheck + lint), built, and hotfixed:

1. Configurable keybinding for "Convert to delimited text"
The command's shortcut became user-configurable under Settings → Shortcuts, instead of a hardcoded binding.
shortcutRegistry.ts, EditorSettingsDialog.vue

2. VS Code snippets importer
Settings → Snippets → Import: reads VS Code snippets JSON (.json / .code-snippets), parses prefix/body pairs, auto-renames on prefix collisions, size-caps the file, and merges into the snippet draft without touching saved settings until Apply. (The Toad XML → JSON conversion for your templates_10092026.json was a one-off data artifact for this importer, not source code.)
vscodeSnippetsImport.ts, EditorSettingsDialog.vue

3. Bulk snippet delete
Checkbox column + "select all" in the snippets list, with a confirm dialog for deleting multiple selected snippets at once.
EditorSettingsDialog.vue

4. DBeaver-style FK drill-through
In any result grid, FK cells get a clickable arrow icon (hover, in both canvas and DOM render modes) plus a context-menu entry that jumps to the referenced parent row — e.g. orders.useruid → the matching users row. Opens in the same pane (reuses the tab on table tabs; runs the lookup in-place on query tabs without clobbering the editor SQL), forward FKs only, works on JOIN results via strict per-source FK resolution. Postgres initially showed no icons on query results: unqualified sources (FROM orders) are deliberately kept schema-less for search_path resolution, and the postgres FK query matches the schema name exactly — n.nspname = '' returns nothing, so every query-tab result silently resolved zero FKs (MySQL never showed this because it folds the schema into the database). The backend now defaults an empty schema to public, matching how the same file's column/index/constraint queries already treat unqualified names. Also fixed ghost columns after following a link on postgres: the in-pane drill-through SELECT had large-value preview marker columns (__DBX_LARGE_VALUE_BYTES_*) injected into it, and the backend strips those only for table-data executions — on a query tab they showed up as extra columns. The drill lookup no longer injects them (a single parent row doesn't need the multi-row preview budget), so the drilled view — and its sort/refresh re-runs — show only real columns.
dataGridForeignKeyNavigation.ts, useDataGridSourceForeignKeys.ts, useNavigationTargets.ts, DataGrid.vue, ContentArea.vue, postgres.rs (list_foreign_keys)

5. Snippets export
Settings → Snippets → Export: saves snippets back to VS Code JSON format (duplicate labels get -2/-3 suffixes; cancel-aware — no success toast when you dismiss the save dialog).
vscodeSnippetsImport.ts (serializeVscodeSnippetsFile), EditorSettingsDialog.vue

6. Index copy-SQL
Table-info drawer → Indexes tab → per-index Copy SQL button: generates a dialect-correct CREATE INDEX (UNIQUE/PK handling, raw key expressions, postgres operator classes and USING method, SQL Server CLUSTERED/NONCLUSTERED + INCLUDE, partial-index WHERE, per-dialect quoting incl. the JDBC quote-only-when-required mode). Hidden for MongoDB.
indexCreateSql.ts, DataGridTableInfoPanels.vue, DataGrid.vue

7. Foreign-key copy-SQL
Table-info drawer → Foreign Keys tab → per-constraint Copy SQL button: generates ALTER TABLE … ADD CONSTRAINT … FOREIGN KEY … REFERENCES …, merging the per-column metadata rows of composite FKs into one statement (grouped from the unfiltered list so drawer search filters can't truncate it), with ON DELETE/ON UPDATE actions (NO ACTION omitted) and cross-schema reference qualification. Hidden for MongoDB.
foreignKeySql.ts, DataGridTableInfoPanels.vue, DataGrid.vue

8. Enter-to-confirm in the delimited-list dialog
The "Convert to delimited list" dialog now confirms on Enter. The dialog body is wrapped in a form whose submit handler runs Confirm, and Confirm is the only submit button (Cancel and Copy are type="button"), so pressing Enter in any field — including immediately after opening, since focus lands in the first input — applies the conversion. Enter remains inert while the preview is empty (Confirm disabled), and Escape still closes.
DelimitedListDialog.vue

9. Constraints copy-SQL
Table-info drawer → Constraints tab → per-constraint Copy SQL button: generates ALTER TABLE … ADD CONSTRAINT. Uses the server-reported definition verbatim when it is a complete body (postgres pg_get_constraintdef), wraps a bare check expression in CHECK (…), and otherwise reconstructs the body from the constraint type, columns, and reference metadata (UNIQUE/PK/EXCLUDE column lists; full FOREIGN KEY … REFERENCES with ON DELETE/ON UPDATE and deferrability). Constraints with nothing to reconstruct copy as an explanatory SQL comment instead of a broken statement. Hidden for MongoDB.
constraintSql.ts, DataGridTableInfoPanels.vue, DataGrid.vue

10. Alphabetical column sort in transposed row view
The transpose view header has an A–Z toggle button: sorting the rows (column names) alphabetically to find a column in a wide table, with numeric collation (col2 before col10) and case-insensitive order. Toggling again restores the table's column order; the sort resets when the transpose view closes. The button sits left in the toolbar (next to the mode badge, before the flexible spacer) — in the narrower SQL result pane the right-side action cluster gets pushed out of view, which hid it when it lived there.
dataGridTranspose.ts (sortTransposeRowsByColumn), DataGrid.vue

11. Dark-mode native dropdown fix (two passes)
Native <select> popups (e.g. the CSV import column-mapping target dropdown) rendered with a light surface under the app's light text in dark mode, making options unreadable. First pass declared color-scheme (light on :root, dark on .dark) in the base tokens — correct defense-in-depth for native control chrome, but it turned out the app already sets color-scheme inline on <html> at runtime (useTheme), and the WebView2 runtime still paints the option list itself with light colors. Second pass forces the popup contents: in dark mode every <option> of a native select gets the popover background/foreground tokens, which Chromium honors inside browser-rendered select popups — app-wide, so all 30+ native selects get readable dark option lists. Light mode is untouched (OS-native popup).
tokens.css, globals.css, useTheme.ts (verified, not changed)

12. Snippet filter in Settings
Settings → Snippets has a filter box above the list: type to narrow the snippets by label, prefix, or SQL body (case-insensitive), with a live "Showing X of Y" count, an X button to clear, and a "no match" row when nothing fits — no more scrolling through hundreds of imported templates to find one for editing. The header select-all checkbox follows the filter (it selects/deselects exactly the visible rows), while rows hidden by the filter keep their selection, so the Delete Selected count always reflects what you actually checked.
EditorSettingsDialog.vue

13. VS Code-style Ctrl+D word/occurrence selection in the SQL editor
Ctrl+D in the query editor now works like VS Code: the first press selects the word at the caret, each further press adds the next occurrence of that word as another cursor+selection, and the multi-selections copy/paste normally with Ctrl+C/Ctrl+V (each instance lands on its own line). The occurrence-selection commands already existed in the app but were defaulted to IntelliJ-style keys (Alt+J / Ctrl+G); the default is now VS Code's Ctrl+D. "Duplicate line" previously owned Ctrl+D and runs the exact same command as "Copy line down" (Shift+Alt+↓, unchanged), so it is now unbound by default and still assignable in Settings → Shortcuts. Settings saved with the old defaults migrate automatically to the new pairing; anything you had customized yourself is untouched. "Select all occurrences" remains at Ctrl+Alt+Shift+J, also rebindable. (An interim caret-word background tint that shipped earlier was removed — not what was wanted.)
shortcutRegistry.ts, codemirrorSearchKeymap.ts, queryEditorOccurrenceSelection.ts (pre-existing), QueryEditor.vue

14. Enter-to-execute in the SQL parameter dialog
Pressing Enter in the SQL parameter dialog now executes the query instead of wiping the entered values. The dialog previously auto-focused its first focusable element — the "Clear values" header button — so an Enter right after the dialog popped activated it and cleared every parameter. The dialog body is now wrapped in a form whose submit runs Execute (Execute is the only submit button; Cancel, Copy, Ignore, and the header actions are all type="button"), and initial focus goes to the first parameter value input instead (with its contents selected, ready to type over a remembered value).
SqlParameterDialog.vue

15. Postgres foreign-server (remote links) browser
Postgres connections get a "Foreign Servers" group in the sidebar tree (next to Extensions, same databases: postgres family incl. HighGo/Vastbase via the agent fallback). Expanding it lists every cataloged remote link (pg_foreign_server — the postgres equivalent of Oracle's DB Links, since the dblink extension itself has no persistent link catalog); clicking one opens a details dialog with its FDW wrapper, owner, server type/version, connection options (host, port, dbname, ...), the server comment, its user mappings (pg_user_mappings, PUBLIC shown for the public mapping), and the foreign tables mounted through it. A "Copy CREATE SERVER SQL" button reconstructs the CREATE SERVER statement (TYPE/VERSION as string literals, options as quoted literals, identifiers properly escaped) plus ALTER SERVER … OWNER TO; user mappings are deliberately excluded from the generated SQL because pg_user_mappings hides passwords from non-privileged viewers, so the DDL would silently drop credentials.
types.rs (ForeignServerInfo), postgres.rs (list_foreign_servers + catalog SQL pin test), schema.rs/_core, commands/schema.rs, lib.rs, dbx-web routes, postgresForeignServers.ts (DDL builder), ForeignServerDetailsDialog.vue, connectionStore.ts, SidebarTreeRuntimeHost.vue, treeNodeClick/Icon/Group.ts, sidebarTreeItemLayout.ts, ConnectionTree.vue

16. Enter-to-confirm and Escape-to-close in the danger confirmation dialogs
Every destructive confirmation (row UPDATE and DELETE confirms in the grid, drop/truncate/empty table, batch drops, read-only unlock, the sidebar's type-the-name confirms, …) now confirms on Enter: the shared dialog's body is wrapped in a form whose submit runs Confirm, with the destructive Confirm button as its only submit control (Cancel, Cancel Query, and the copy/wrap helpers are type="button"), matching the SQL parameter and delimited-list dialogs. Enter stays inert while the statement is running or the caller's own gate (e.g. typed-name match) holds the button back. Escape closes the dialog through the framework's built-in handling — now pinned by tests — and deliberately keeps it open while a destructive query is running (Cancel Query is the way out there). To make this safe, the shared Button component now defaults to type="button" (submit must be opted into with type="submit"); previously any untyped Button rendered inside a form silently became a submit control, so e.g. clicking a Copy button on an error banner inside a dialog would have natively submitted the form and confirmed the destructive action.
DangerConfirmDialog.vue, Button.vue, __tests__/DangerConfirmDialog.spec.ts

17. SQL parameter dialog stays open when clicking outside it
The parameter dialog is now modeless: clicking in the SQL editor (or anywhere outside the box) no longer dismisses it, and the dark overlay is gone, so the editor behind stays fully interactive — text can be selected and copied into a parameter while the dialog is on screen. The box keeps its position, Escape/Cancel/Execute/Ignore still close it, and Enter still executes (feature 14). Fix included: Escape previously did nothing while the value-history suggestion popover was open — the popover's dismissal never reached its controlled open state, and as the topmost layer it swallowed every keypress; Escape now closes the suggestions first and the dialog on the next press.
SqlParameterDialog.vue, __tests__/SqlParameterDialog.spec.ts

18. F3 opens the database selector dropdown
F3 (rebindable in Settings → Shortcuts → Global, "Open database selector dropdown") opens the database selector at the top right of the query editor toolbar — the dropdown pops with its filter input focused and the current database highlighted, so typing filters immediately, arrow keys + Enter pick. Precedence is unchanged where F3 already meant something: while focus is in the results grid F3 still pages to the next page (the grid's own listener), and while the editor's search panel is open F3 still means find-next. The latter needed a real fix: CodeMirror's stock search keymap carries preventDefault on its F3 binding, which marks the key handled even when the handler does nothing — so with the search panel closed, F3 in the editor was silently swallowed and never reached the global handler. The binding is now conditional (find-next only while the panel is open) and no longer prevents the default when it declines, letting F3 bubble up to open the selector. On tabs without a database selector (single-database connection types) F3 falls through untouched. The F3 overlap with the grid-scope next-page shortcut is cross-scope and stays informational, like the Mod+F find/focus-search pair.
shortcutRegistry.ts, keyboardShortcuts.ts, codemirrorSearchKeymap.ts, App.vue, SqlEditorWorkspace.vue, EditorGroup.vue, EditorToolbar.vue, SearchableSelect.vue, QueryEditor.vue, SqlFormatterSettingsPanel.vue

19. CSV export options dialog
Exporting a SQL result or table data as CSV (including "Export Selected Rows as CSV" and current-page CSV) now opens an options dialog first: quoting mode (quote all fields / only when needed / off), delimiter (comma, semicolon, tab, pipe, or any custom single character), quote character (double, single, backtick, or custom), and an include-header-row toggle. "Only when needed" quotes a field only when it contains the chosen delimiter, the chosen quote character, or a line break; "off" writes everything raw. The tab delimiter saves as .tsv (save-dialog filter follows). The quote-mode radio defaults from the existing Settings → Editor CSV quote setting, and the per-export choice is not written back. All three export paths honor the options — the backend streaming query-result export and table export (Rust now threads a CsvTextFormat of quote mode + delimiter + quote char + header switch through every formatter and writer, with serde defaults so old payloads keep working) and the in-memory frontend fallback (Tauri command and the web build's formatter alike). Enter confirms (form wrapper, Confirm is the only submit button), Escape/Cancel aborts the export; a custom delimiter/quote character must be entered before Confirm enables.
CsvExportDialog.vue, csvExportDialog.ts, csvQuoteMode.ts, exportFormats.ts, useDataGridExport.ts, tauri.ts, http.ts, legacyWebViewRegexCompat.spec.ts, en.ts, zh-CN.ts, csv_export.rs (dbx-formats), query_result_export.rs, table_export.rs, csv_export.rs (dbx-core), commands/csv_export.rs (src-tauri)

Upstream later shipped its own settings-based quote mode (merged in the v0.6.19 sync, threading csvQuoteMode through the same export chain), so the 2026-09-22 sync reconciled the layers: upstream's string-typed signatures are kept — exportQueryResultCsv's 4th argument is still the bare quote mode with the dialog's delimiter/quote-char/header options riding as a new 5th argument, and formatCsv accepts either a bare mode or the options object — while the pieces upstream's implementation already covers (ObjectBrowser.vue and useSidebarTreeExportRuntime.ts mode threading) were dropped back to the upstream versions. That sync also fixed an upstream Windows-only test bug: the WebView lookbehind-compat spec compares its forward-slash allowlist against path.relative's backslash output, so the sole allowed exception was flagged as an offender on every Windows checkout.

20. Double-Shift database object search
Pressing Shift twice quickly (JetBrains Search Everywhere style, within 400 ms, with no other key in between — so typing capitals never trips it) opens a search dialog scoped to the active database: it lists every table, view, materialized view, procedure, function, and trigger the connection type supports, and you filter by typing (same matching as Ctrl+P quick-open: exact → initials → prefix → pinyin initials → word-prefix → substring → fuzzy, with highlighted matches and type badges). Trigger support follows the connection's capabilities — MySQL-family and Kingbase/Xugu list database-wide triggers, PostgreSQL-family doesn't list them at the database level so they don't appear there. Selecting a table/view/materialized view opens it as a data tab; procedures, functions, and triggers open their source in an editor tab (Ctrl+P quick-open's open handlers are reused as-is, including overload signatures). The scope is the active tab's connection + database + schema + catalog, falling back to the active connection's configured database. Fix for schema-aware engines (postgres family, SQL Server, …): object listing is schema-scoped there (`n.nspname = ''` matches nothing), and a tab that never picked a schema produced a blank schema and therefore an empty list — the dialog now fans out over the database's schemas (parallel queries, capped at 50, a failing schema doesn't lose its siblings, error only when all fail) and merges the results, so the search covers the whole database; database-object-tree engines (dialect-less JDBC, mongodb) keep the single whole-database call. The schema list is filtered through the sidebar's own system-schema filter before the cap, because a postgres database carries one numbered pg_toast_N system schema per table, they sort ahead of every user schema, and the first 50 would otherwise all be empty system schemas. That also exposed an upstream blind spot: the shared postgres-like system-schema rules knew the exact name pg_toast and the pg_temp_/pg_toast_temp_ prefixes but not the numbered pg_toast_ prefix (the backend's hide-system catalog SQL misses it too), so numbered toast schemas leaked into the sidebar's schema tree and the schema picker — they're now classified as system everywhere. The gesture is observed on a window capture listener (so keydowns stopped by other handlers still reset it), resets on window blur, and does nothing while another dialog is open.
doubleShiftGesture.ts, useDatabaseObjectSearch.ts, DatabaseObjectSearchDialog.vue, App.vue, visibleDatabases.ts, en.ts, zh-CN.ts

21. Focus moves to the query editor after picking a database with F3
Opening the database selector with F3 (feature 18) and picking a database — Enter or click — now hands focus to the query editor automatically, so F3 → type → Enter lands you straight in the editor without touching the mouse. Only selections from the F3-opened dropdown do this: opening the dropdown by clicking the trigger behaves exactly as before (focus returns to the trigger), and dismissing the F3 dropdown with Escape or an outside click cancels the hand-off. The focus emit is deferred one animation frame so it lands after the popover's own focus restore to the trigger.
EditorToolbar.vue, EditorGroup.vue, QueryEditorSurface.vue, ContentArea.vue, querySurfaces.ts, QueryEditor.vue, __tests__/EditorToolbar.databaseSelectFocus.spec.ts

22. Ctrl+Shift+C commits the transaction
The toolbar's commit button now has a keyboard shortcut — Ctrl+Shift+C by default (Cmd+Shift+C on macOS), rebindable in Settings → Shortcuts → Global as "Commit transaction". It acts on the active editor group's tab, the same tab the toolbar commit button would, and only consumes the keys when that tab actually holds an open transaction (manual transaction mode, or an auto-commit tab that kept an explicit BEGIN); with no transaction open the combination stays free for anything more specific bound to it.
shortcutRegistry.ts, keyboardShortcuts.ts, EditorGroup.vue, SqlEditorWorkspace.vue, App.vue, en.ts, zh-CN.ts, __tests__/shortcutRegistry.spec.ts, __tests__/keyboardShortcuts.spec.ts

23. $0 final cursor position in snippets
VS Code-style $0 in a snippet body now marks where the cursor should land: `SELECT * FROM ${1:table} WHERE $0` puts the caret at the WHERE position once you've filled (or Tabbed past) the table field. ${1:default} tabstops with Tab/Shift+Tab navigation already worked — dbx snippets and the VS Code importer share that syntax — but CodeMirror's snippet engine has no $0 concept: a bare $0 was inserted literally, and ${0} sorted as the *first* field because fields order by number. $0/${0} are now renumbered to one past the highest tabstop, which is the final field in CodeMirror: Tab walks the numbered fields, then the cursor lands there and snippet mode ends, matching VS Code. Multiple $0 occurrences mirror into one final field (multi-caret). Only zero is translated — bare $1/$2 stay literal because they are PostgreSQL bind parameters, and \$0 stays a literal dollar.
sqlCompletion.ts, __tests__/sqlCompletion.snippet.spec.ts

24. Add-snippet dialog from the editor's right-click menu
The query editor's context menu has an "Add snippet" entry (next to "Convert to delimited list") that opens a small dialog to create a snippet on the spot: label (optional, defaults to the prefix), trigger prefix, and SQL body. When the menu was opened on a selection, the body comes prefilled with the selected SQL — select a statement, right-click, add snippet, type a prefix, Enter, done. Saving writes immediately (unlike Settings → Snippets' Apply-based draft list, which stays the full editor); a toast confirms the save, and the snippet is available in completions right away. The prefix must be non-empty and unique — duplicates are flagged live with the store's own rule kept honest (its normalizer silently keeps the first of duplicate prefixes, so the dialog rejects a clash before it can reach the store). Enter in any field confirms (form wrapper, Save is the only submit button; disabled until the prefix is valid), Escape/Cancel close without saving, and focus lands on the prefix field since that's the required one. The entry is deliberately always enabled: saving a snippet never touches editor content, so it works in read-only editors and without a selection (the body just starts empty).
SnippetQuickAddDialog.vue, QueryEditor.vue, en.ts, zh-CN.ts, __tests__/SnippetQuickAddDialog.spec.ts, __tests__/QueryEditor.snippetQuickAdd.spec.ts

25. Double-Shift search opens the object definition, with a View button for data
Selecting a table, view, or materialized view in the double-Shift database search now shows the object's source/specification instead of listing its content: tables open the table structure editor straight on its DDL tab, and views/materialized views open the DDL view dialog with the CREATE statement — the same two surfaces the editor's Ctrl+click DDL mode uses, including the rule that keeps views out of the table editor. Each result row also carries two buttons: "Source" (the default — row click and Enter do the same) and "View", which opens the object's data tab, the search's original behavior; routines have no data view, so their rows offer Source only. The footer's Enter hint follows the default. The connection is activated and connected first, exactly like the old data path. Deliberate scope: this is a hard behavior change with no setting (the requested preference), and Ctrl+P quick-open — which shares the app with the search — still opens data tabs, since only the database search got its own select handler.
App.vue, DatabaseObjectSearchDialog.vue, en.ts, zh-CN.ts, __tests__/DatabaseObjectSearchSourceNavigation.spec.ts, __tests__/DatabaseObjectSearchDialog.spec.ts

26. Unfilled SQL parameters send NULL
Leaving a value empty in the run-query parameter dialog now substitutes NULL instead of an empty string literal: `WHERE name = :name` left blank matches nothing, instead of silently matching every row where name is the empty string. This covers the String kind (the default) and Boolean; Number and Null already rendered empty as NULL. Raw keeps its own contract — an empty Raw value still leaves the placeholder unresolved in the SQL, and typing `''` as Raw remains the way to send an explicit empty string. Typed whitespace is still data (a single space sends `' '`), and one placement is deliberately excepted: a placeholder embedded inside a larger string literal ('file_${name}.txt') still splices in nothing when empty, because NULL cannot live inside a literal; a placeholder that IS the whole literal ('${name}') renders NULL when empty. The value field's placeholder text now says "Enter value (empty sends NULL)" in both locales, so the rule is visible exactly when a field is empty, and the SQL preview below the table has always shown the resolved statement. Clear Values therefore also produces all-NULL parameters (Raw excepted).
sqlParameters.ts, __tests__/sql/sqlParameters.spec.ts, __tests__/SqlParameterDialog.spec.ts, en.ts, zh-CN.ts

27. GitHub Copilot as a first-class AI provider
Settings → AI has a "GitHub Copilot" provider entry (with the GitHub mark logo, inverted in dark mode) alongside OpenAI/Claude/etc.: picking it pre-fills the endpoint (https://api.githubcopilot.com/chat/completions), Bearer auth, and a gpt-4.1 default model, so only the token field is left to fill — a GitHub PAT from an account with an active Copilot subscription (paid seat or Copilot Free), which the endpoint hint spells out. The API is OpenAI chat-completions shaped with one trap: it sits directly under the host with no /v1 prefix, unlike every other OpenAI-style provider where dbx auto-adds /v1 to a bare origin — so endpoint and model-list resolution special-case Copilot (a typed origin gets /chat/completions directly, and the model picker lists /models at the root rather than /v1/models). Model discovery therefore works out of the box, reasoning-effort capabilities follow the OpenAI model registry (Copilot serves OpenAI models), the API-style switcher stays hidden (chat completions only, enforced backend-side like MiniMax), and the key is required. Everything downstream — chat, agent streaming, test connection — rides the existing OpenAI-compatible request path with Authorization: Bearer <PAT>.
types/ai.ts, settingsStore.ts, AiProviderLogo.vue, public/icons/ai/githubcopilot.svg, EditorSettingsDialog.vue, en.ts, zh-CN.ts, __tests__/aiConfigOrdering.spec.ts, __tests__/settingsStore.spec.ts, ai.rs, ai_effort.rs, ai_model_filter.rs (crates/dbx-ai-provider)

2026-09-24 upstream sync (v0.6.21, 61 commits): upstream split QueryEditor.vue into ~25 composables plus a new QueryEditorContextMenu.vue that now owns the right-click menu, so three mods were re-homed onto the new architecture: mod 24's "Add snippet" entry and mod 1's shortcut hint on the delimited-list item now live in QueryEditorContextMenu.vue (fed through the new contextMenuActions object), mod 1's keymap binding went into the runKeymapExtension block that stayed in QueryEditor.vue, and mod 24's source-pinning spec reads the new menu file. Upstream had also absorbed the codemirrorSearchKeymap mod under the narrower name searchKeymapWithoutModD (Mod+D strip only); the mod's richer searchKeymapWithoutShortcutConflicts (Mod+D strip + the conditional F3 from mod 18) was kept and upstream's call site re-pointed at it. Upstream deleted the old delimited-list source-pinning test in queryEditorTextEdits.spec.ts — its mod-1 extension went with it. Everything else merged clean: ForeignServerInfo (mod 15) kept beside upstream's ObjectStatistics change in types.rs, both-sides-added locale blocks (upstream's structurePeek, mod 24's snippetQuickAdd) coexist, and the Copilot provider (27) survived untouched. Verified: full frontend suite 13,577/13,582 — the 5 failures (ExportProgressPopover number grouping, nacos namespace order, redis subtree-fill rotation) fail identically on the pre-merge tree, i.e. machine-locale/ordering quirks that pre-date the sync, not regressions; typecheck, oxlint, cargo check --workspace, and dbx-ai-provider's 266 tests all clean.